commands:
  echoTestSuccess:
    cmd: echo
    shell: bash
    Args: hello world
    hooks:
      success:
        - successCmd

  successCmd:
    name: get docker version
    cmd: docker
    output:
      file: docker_version_success.txt
      toLog: true
    Args:
      - "-v"